name: feature_workspace_api
publish_to: 'none'

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  diff_lint_rules:
    path: ../../../diff_lint_rules

flutter: